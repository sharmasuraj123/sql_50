select query_name,Round(SUM(rating/position)/count(query_name),2) as quality,Round(100.0*SUM(CASE WHEN rating<3 THEN 1 ELSE 0 END)/count(query_name),2) as poor_query_percentage from Queries group by query_name;