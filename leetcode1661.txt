
select a.machine_id,
ROUND(
    
        SUM(CASE WHEN a.activity_type = 'end' then a.timestamp 
        ELSE -a.timestamp END)
        /(COUNT(*)/2)
    ,3) as processing_time
    from Activity as a 
    group by a.machine_id;