
select a.user_id,ROUND(IFNULL(SUM(Case when b.action = 'confirmed' then 1 else 0 END)/COUNT(b.action),0),2) as confirmation_rate
from Signups as a
left join
Confirmations as b on a.user_id = b.user_id

group by a.user_id;